# Redirect ALL DNS queries to the Pi's gateway IP.
# This makes every hostname resolve to 192.168.4.1, which causes iOS, Android,
# and macOS to detect a captive portal and automatically open the browser.
address=/#/192.168.4.1
